import getBaseTranslator from './getBaseTranslator';
import useHook from './useHook';
import useLocale from './useLocale';
export default function useTranslations(...[namespace]) {
    const locale = useLocale();
    const result = useHook('useTranslations', getBaseTranslator(locale, namespace));
    return result;
}
//# sourceMappingURL=useTranslations.js.map